<%= form_for [@story, @story.snippets.build], :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label 'What is next?', :class => 'control-label' %>
    <div class="controls">
      <%= f.text_area :content, :class => 'text_area, input-block-level' %>
    </div>
  </div>
  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                root_path, :class => 'btn' %>
				
			<h6> ******* CLICKING ON Create Snippet IS MANDATORY AT THE MOMENT, ELSE THE DB IS LOCKED FOR EVER *******	
				
  </div>
<% end %>
