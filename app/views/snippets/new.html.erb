<h4>What happens next?</h4>
<hr />

<h2 class="snippet"><%= @story.title %></h2>


<h2>
<% if @story.snippets.first.content != nil %>
 <%= @story.snippets.order("created_at ASC").last.content%>
<% else %> 
 Hey dude, you have the honor of beginning this story!
<% end %>
</h2>

<h6>Now it is up to you...</h6>




<%= render 'form' %>

<%= link_to 'Back', story_snippets_path(@story) %>
