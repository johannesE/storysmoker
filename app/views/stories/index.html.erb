<h1>All the stories...</h1>

<table>
  <thead>
  <tr>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
  </thead>
 
  <tbody>
  <% @stories.where(editable: '0').each do |story| %>
      <tr>
        <td><%= story.title %></td>
        <td><%= link_to 'Show | ', story %></td>

        <td><%= link_to 'Edit  ', edit_story_path(story) %></td>

      </tr>
  <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Story', new_story_path %>
