<%- model_class = Story -%>

<div class="page-header handWritten">
  <h1><%= @story.title %></h1>
 
  
</div>


<div class="handWritten">

<%= render @story.snippets.sort_by &:created_at %>

</div>

<% @story.tags.each do |tag| %>
 <%= link_to tag.name, tagged_url(:tag => tag.name) %>
<% end %>  



